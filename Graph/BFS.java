// Bfs of a graph

/**
 * Uses a queue.
 * Add the root to the queue.
 * traverse the queue.
 * mark the size of the queue and traverse the given size only(while(size-->0))
 * remove the head and add all the children of the nodes to the queue.
 * traverse till queue is not empty. 
*/
