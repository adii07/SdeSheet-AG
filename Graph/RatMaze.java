// Rat in maze

// rat enters the maze from the top left corner, now it can move down or right on only those cells which have 1
// print the path the rat can take to leave the matrix from any cell on the right.

/**
 * Again make use of dfs and make the row-1 call with 'd' and right call with 'r'
 * when col>=grid[0].length print the path.
*/