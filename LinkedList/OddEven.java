//Segregate the odd even numbers in the linked list

/**
 * Use 2 dummy heads even and odd
 * Add the even numbers to the even chain
 * Add the odd numbers to the odd chain
 * Mark the next of the even pointer to point to the head of the odd list
 * Makr the next pointer of the odd pointer as null to avoid any cycle formation
 * 
*/